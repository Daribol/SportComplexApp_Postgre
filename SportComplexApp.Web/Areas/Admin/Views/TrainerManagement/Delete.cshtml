@using SportComplexApp.Web.ViewModels.Trainer
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model DeleteTrainerViewModel

@{
    ViewData["Title"] = Localizer["Title"];
}

<div class="card card-dark shadow-lg border-0 rounded-4 mb-4">
    <div class="card-body p-4 p-md-5">
        <h2 class="mb-3" style="color:#ff4d4d;">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> @Localizer["Heading"]
        </h2>

        <div class="alert alert-dark border border-warning d-flex align-items-start gap-2 mb-4">
            <i class="bi bi-exclamation-circle-fill text-warning mt-1"></i>
            <div>
                <strong class="text-warning">@Localizer["WarningTitle"]</strong><br />
                @Localizer["WarningText"]
                <span class="fw-bold" style="color:#ff6b6b;">@Model.Name @Model.LastName</span>
                <br />@Localizer["WarningUndone"]
            </div>
        </div>

        <form asp-action="ConfirmDelete" method="post" class="mt-2 d-flex gap-2">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">
                <i class="bi bi-trash3 me-1"></i> @Localizer["BtnDelete"]
            </button>
            <a asp-action="All" class="btn btn-outline-neon">
                <i class="bi bi-x-circle me-1"></i> @Localizer["BtnCancel"]
            </a>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
}