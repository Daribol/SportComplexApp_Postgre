@using SportComplexApp.Web.ViewModels.Tournament
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model IEnumerable<TournamentViewModel>

@{
    ViewData["Title"] = Localizer["Title"];
    var now = DateTime.Now;
}

<div class="container mt-4">
    <h2 class="mb-3 text-center" style="color:#00ffc8;">@Localizer["Heading"]</h2>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>@TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>@TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (!Model.Any())
    {
        <div class="card card-dark text-center p-5 border-0">
            <div class="display-6 mb-2">🏆</div>
            <h5 class="mb-1">@Localizer["NoTournamentsTitle"]</h5>
            <p class="text-muted mb-0">@Localizer["NoTournamentsDesc"]</p>
        </div>
    }
    else
    {
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            @foreach (var t in Model)
            {
                var isFinished = t.EndDate < now;
                var isUpcoming = t.StartDate > now;
                
                var statusKey = isFinished ? "StatusFinished" : (isUpcoming ? "StatusUpcoming" : "StatusOngoing");
                var badgeClass = isFinished ? "badge-soft" : (isUpcoming ? "badge-sport" : "bg-success");
                
                var durationDays = Math.Max(1, (t.EndDate.Date - t.StartDate.Date).Days + 1);

                var imgProp = t.GetType().GetProperty("ImageUrl");
                var imageUrl = imgProp?.GetValue(t)?.ToString();

                <div class="col">
                    <div class="card card-dark hover h-100 border-0">
                        @if (!string.IsNullOrWhiteSpace(imageUrl))
                        {
                            <div class="cover ratio ratio-16x9" style="background-image:url('@imageUrl');"></div>
                        }
                        else
                        {
                            <div class="cover cover--placeholder ratio ratio-16x9 d-flex align-items-center justify-content-center">
                                <i class="bi bi-trophy-fill display-6 text-muted"></i>
                            </div>
                        }

                        <div class="card-body d-flex flex-column">
                            <div class="d-flex align-items-start justify-content-between mb-2">
                                <h5 class="card-title mb-0">@t.Name</h5>
                                <span class="badge @badgeClass">
                                    @Localizer[statusKey]
                                </span>
                            </div>

                            <div class="mb-2">
                                <span class="badge badge-sport me-1">@t.Sport</span>
                                <span class="badge badge-soft">@durationDays @Localizer["DaysShort"]</span>
                            </div>

                            <ul class="list-unstyled small text-muted mb-3">
                                <li><i class="bi bi-calendar-event text-primary"></i> @t.StartDate.ToString("dd MMM yyyy") → @t.EndDate.ToString("dd MMM yyyy")</li>
                            </ul>

                            @if (!string.IsNullOrWhiteSpace(t.Description))
                            {
                                <p class="text-muted clamp-3 mb-0">@t.Description</p>
                            }

                            @if (t.StartDate > now)
                            {
                                <form asp-action="Unregister" method="post" class="mt-3">
                                    <input type="hidden" name="id" value="@t.Id" />
                                    <button type="submit" class="btn btn-outline-neon btn-sm w-100">
                                        <i class="bi bi-x-circle me-1"></i> @Localizer["BtnUnregister"]
                                    </button>
                                </form>
                            }
                            else
                            {
                                <div class="mt-3">
                                    <button class="btn btn-outline-neon btn-sm w-100" disabled>
                                        @Localizer["MsgRegistrationClosed"]
                                    </button>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}